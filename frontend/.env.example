# Copy to .env.local for local dev, or set in Vercel for production.
# Local backend (uvicorn) — note port 8001 in this setup:
VITE_API_BASE_URL=http://127.0.0.1:8001
# Production example (set this in the Vercel dashboard):
# VITE_API_BASE_URL=https://nike-ai-backend.onrender.com
